/** Formatting is uniform across the monorepo, so diffs stay reviewable. */
export default {
  semi: true,
  singleQuote: true,
  trailingComma: 'all',
  printWidth: 100,
  arrowParens: 'always',
  plugins: ['prettier-plugin-tailwindcss'],
};
